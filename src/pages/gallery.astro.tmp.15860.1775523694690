---
import { Image } from "astro:assets";
import BaseHead from "../components/BaseHead.astro";
import Footer from "../components/Footer.astro";
import Header from "../components/Header.astro";
import { SITE_TITLE } from "../consts";

// Import artwork images
import morganaAndAnna from "../assets/gallery/art/morgana-and-anna.png";
import beaImg from "../assets/characters/bea.png";
import jWooImg from "../assets/characters/j-woo.png";

// Artwork images
interface ArtworkImage {
  image: ImageMetadata;
  alt: string;
  title: string;
}

const artworkImages: ArtworkImage[] = [
  { image: morganaAndAnna, alt: "Morgana and Anna concept art", title: "Morgana & Anna" },
  { image: beaImg, alt: "Bea Beatrix character art", title: "Bea Beatrix" },
  { image: jWooImg, alt: "J-Woo character art", title: "J-Woo" },
];
---

<!doctype html>
<html lang="en">
  <head>
    <BaseHead
      title={`Gallery - ${SITE_TITLE}`}
      description="Gameplay footage and artwork from EIDOLWARE"
    />
  </head>
  <body>
    <Header />
    <main class="gallery-main">
      <!-- Hero -->
      <section class="hero">
        <div class="hero-bg">
          <div class="hero-grid"></div>
          <div class="glow glow-pink"></div>
          <div class="glow glow-cyan"></div>
          <div class="scanlines"></div>
        </div>

        <div class="hero-inner">
          <div class="hero-top-bar">
            <span class="bar-label">EIDOLWARE //</span>
            <span class="bar-jp">ギャラリー</span>
          </div>

          <h1 class="hero-title">Gallery</h1>
          <div class="tagline">
            <div class="tag-line"></div>
            <span class="tag-text">Visual Showcase</span>
            <div class="tag-line"></div>
          </div>
        </div>

        <div class="hero-stripe">
          <div class="stripe-pink"></div>
          <div class="stripe-cyan"></div>
        </div>
      </section>

      <!-- Media Section -->
      <section class="content-section">
        <div class="section-head">
          <div class="num-block num-pink">
            <span class="num">01</span>
          </div>
          <div class="head-text">
            <span class="head-jp">メディア</span>
            <h2 class="head-title">Media</h2>
          </div>
          <div class="head-rule"></div>
        </div>

        <div class="section-body">
          <div id="youtube-media" class="media-grid">
            <div class="coming-soon loading-placeholder">
              <span>Loading videos…</span>
            </div>
          </div>
          <div id="youtube-see-more" class="see-more" style="display:none;">
            <a
              href="https://www.youtube.com/@fatbatstudio"
              target="_blank"
              rel="noopener noreferrer"
              class="see-more-link"
            >
              See more on YouTube →
            </a>
          </div>
        </div>
      </section>

      <!-- Artwork Section -->
      <section class="content-section">
        <div class="section-head">
          <div class="num-block num-cyan">
            <span class="num">02</span>
          </div>
          <div class="head-text">
            <span class="head-jp">アートワーク</span>
            <h2 class="head-title">Artwork</h2>
          </div>
          <div class="head-rule"></div>
        </div>

        <div class="section-body">
          {
            artworkImages.length > 0 ? (
              <div class="media-grid">
                {artworkImages.map((artwork, index) => (
                  <button
                    class="media-card"
                    data-lightbox-trigger
                    data-index={index}
                    type="button"
                  >
                    <Image
                      src={artwork.image}
                      alt={artwork.alt}
                      width={600}
                      height={800}
                      class="media-img"
                      loading="lazy"
                    />
                    <div class="media-caption">
                      <span>{artwork.title}</span>
                      <span class="zoom-hint">Click to enlarge</span>
                    </div>
                  </button>
                ))}
              </div>
            ) : (
              <div class="coming-soon">
                <span>Artwork coming soon…</span>
              </div>
            )
          }
        </div>
      </section>

      <!-- Lightbox -->
      <div class="lightbox" id="lightbox" aria-hidden="true">
        <div class="lightbox-backdrop"></div>
        <button class="lightbox-close" aria-label="Close lightbox">&times;</button>
        <div class="lightbox-content">
          <img id="lightbox-img" class="lightbox-img" src="" alt="" />
          <div class="lightbox-caption" id="lightbox-caption"></div>
        </div>
        <button class="lightbox-prev" aria-label="Previous image">&#8249;</button>
        <button class="lightbox-next" aria-label="Next image">&#8250;</button>
      </div>
    </main>
    <Footer />

    <script>
      (async function loadYouTubeMedia() {
        const container = document.getElementById("youtube-media");
        const seeMore = document.getElementById("youtube-see-more");
        if (!container) return;

        try {
          const res = await fetch(
            "https://www.youtube.com/feeds/videos.xml?channel_id=UCLo_cJHih7tCTnAn28sIlxg"
          );
          const xml = await res.text();
          const parser = new DOMParser();
          const doc = parser.parseFromString(xml, "text/xml");
          const entries = Array.from(doc.querySelectorAll("entry")).slice(0, 3);

          if (entries.length === 0) {
            container.innerHTML =
              '<div class="coming-soon"><span>No videos found</span></div>';
            return;
          }

          container.innerHTML = entries
            .map((entry) => {
              const videoId = entry.querySelector("videoId")?.textContent ?? "";
              const title = entry.querySelector("title")?.textContent ?? "";
              return (
                '<div class="media-card video-card">' +
                '<div class="video-wrapper">' +
                '<iframe src="https://www.youtube-nocookie.com/embed/' +
                videoId +
                '" title="' +
                title.replace(/"/g, "&quot;") +
                '" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen loading="lazy"></iframe>' +
                "</div>" +
                '<div class="media-caption"><span>' +
                title.replace(/</g, "&lt;").replace(/>/g, "&gt;") +
                "</span></div></div>"
              );
            })
            .join("");

          if (seeMore) seeMore.style.display = "";
        } catch (e) {
          console.error("YouTube feed error:", e);
          container.innerHTML =
            '<div class="coming-soon"><span>Failed to load videos</span></div>';
        }
      })();
    </script>

    <script define:vars={{ artworkImages: artworkImages.map(a => ({ src: a.image.src, alt: a.alt, title: a.title })) }}>
      document.addEventListener('DOMContentLoaded', () => {
        const triggers = document.querySelectorAll('[data-lightbox-trigger]');
        const lightbox = document.getElementById('lightbox');
        const lightboxImg = document.getElementById('lightbox-img');
        const lightboxCaption = document.getElementById('lightbox-caption');
        const closeBtn = document.querySelector('.lightbox-close');
        const backdrop = document.querySelector('.lightbox-backdrop');
        const prevBtn = document.querySelector('.lightbox-prev');
        const nextBtn = document.querySelector('.lightbox-next');

        let currentIndex = 0;

        function showImage(index) {
          if (index < 0) index = artworkImages.length - 1;
          if (index >= artworkImages.length) index = 0;
          currentIndex = index;

          const artwork = artworkImages[index];
          lightboxImg.src = artwork.src;
          lightboxImg.alt = artwork.alt;
          lightboxCaption.textContent = artwork.title;
        }

        function openLightbox(index) {
          showImage(index);
          lightbox.classList.add('active');
          lightbox.setAttribute('aria-hidden', 'false');
          document.body.style.overflow = 'hidden';
        }

        function closeLightbox() {
          lightbox.classList.remove('active');
          lightbox.setAttribute('aria-hidden', 'true');
          document.body.style.overflow = '';
        }

        triggers.forEach((trigger) => {
          trigger.addEventListener('click', () => {
            const index = parseInt(trigger.dataset.index, 10);
            openLightbox(index);
          });
        });

        closeBtn?.addEventListener('click', closeLightbox);
        backdrop?.addEventListener('click', closeLightbox);
        prevBtn?.addEventListener('click', () => showImage(currentIndex - 1));
        nextBtn?.addEventListener('click', () => showImage(currentIndex + 1));

        document.addEventListener('keydown', (e) => {
          if (!lightbox.classList.contains('active')) return;
          if (e.key === 'Escape') closeLightbox();
          if (e.key === 'ArrowLeft') showImage(currentIndex - 1);
          if (e.key === 'ArrowRight') showImage(currentIndex + 1);
        });
      });
    </script>
  </body>
</html>

<style>
  .gallery-main {
    padding: 0;
    max-width: 100%;
  }

  /* Hero */
  .hero {
    position: relative;
    min-height: 280px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    overflow: hidden;
  }

  .hero-bg {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
  }

  .hero-grid {
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(232, 21, 122, 0.06) 1px, transparent 1px),
      linear-gradient(90deg, rgba(232, 21, 122, 0.06) 1px, transparent 1px);
    background-size: 60px 60px;
    mask-image: radial-gradient(
      ellipse 80% 70% at 50% 40%,
      black 20%,
      transparent 70%
    );
  }

  .glow {
    position: absolute;
    border-radius: 50%;
    filter: blur(140px);
    opacity: 0.12;
  }

  .glow-pink {
    width: 600px;
    height: 600px;
    background: #e8157a;
    top: -30%;
    left: 10%;
  }

  .glow-cyan {
    width: 450px;
    height: 450px;
    background: #00e5c3;
    bottom: -20%;
    right: 5%;
  }

  .scanlines {
    position: absolute;
    inset: 0;
    background: repeating-linear-gradient(
      0deg,
      transparent,
      transparent 2px,
      rgba(255, 255, 255, 0.008) 2px,
      rgba(255, 255, 255, 0.008) 4px
    );
    pointer-events: none;
  }

  .hero-inner {
    position: relative;
    z-index: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    text-align: center;
    padding: 3rem 2rem;
  }

  .hero-top-bar {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    margin-bottom: 2rem;
    padding: 0.5rem 1.5rem;
    border: 1px solid #1a1a28;
    width: fit-content;
  }

  .bar-label {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.75rem;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: #e8157a;
  }

  .bar-jp {
    font-family: "Noto Sans JP", sans-serif;
    font-size: 0.78rem;
    font-weight: 700;
    color: #00e5c3;
    letter-spacing: 0.15em;
  }

  .hero-title {
    font-family: "Bebas Neue", sans-serif;
    font-size: 5rem;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #fff;
    margin: 0 0 1rem;
    text-shadow: none;
    line-height: 1;
  }

  .tagline {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    justify-content: center;
  }

  .tag-line {
    height: 1px;
    width: 60px;
    background: linear-gradient(90deg, transparent, #e8157a);
  }

  .tag-line:last-child {
    background: linear-gradient(90deg, #00e5c3, transparent);
  }

  .tag-text {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.78rem;
    letter-spacing: 0.25em;
    text-transform: uppercase;
    color: #9090a0;
  }

  .hero-stripe {
    display: flex;
    height: 3px;
  }

  .stripe-pink {
    flex: 1;
    background: #e8157a;
  }

  .stripe-cyan {
    flex: 1;
    background: #00e5c3;
  }

  /* Content Sections */
  .content-section {
    max-width: 1200px;
    margin: 0 auto;
    padding: 3rem 2rem;
    border-bottom: 1px solid #12121e;
  }

  .section-head {
    display: flex;
    align-items: center;
    gap: 1.25rem;
    margin-bottom: 2rem;
  }

  .num-block {
    width: 50px;
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
    border: 2px solid;
    flex-shrink: 0;
  }

  .num-pink {
    border-color: #e8157a;
  }

  .num-cyan {
    border-color: #00e5c3;
  }

  .num {
    font-family: "Bebas Neue", sans-serif;
    font-size: 1.5rem;
    color: #fff;
    line-height: 1;
  }

  .head-text {
    display: flex;
    flex-direction: column;
    gap: 0.1rem;
  }

  .head-jp {
    font-family: "Noto Sans JP", sans-serif;
    font-size: 0.7rem;
    font-weight: 700;
    color: #00e5c3;
    letter-spacing: 0.2em;
  }

  .head-title {
    font-family: "Bebas Neue", sans-serif;
    font-size: 1.8rem;
    letter-spacing: 0.08em;
    color: #fff;
    margin: 0;
    text-shadow: none;
    text-transform: uppercase;
  }

  .head-rule {
    flex: 1;
    height: 1px;
    background: linear-gradient(90deg, #1a1a28, transparent);
  }

  .section-body {
    padding-left: 4.5rem;
  }

  /* Media Grid */
  .media-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
    gap: 1.5rem;
  }

  .media-card {
    border: 2px solid #e8157a;
    background: rgba(0, 0, 0, 0.3);
    overflow: hidden;
    transition: all 0.3s ease;
    cursor: zoom-in;
    padding: 0;
    text-align: left;
  }

  .media-card:hover {
    border-color: #00e5c3;
    box-shadow: 0 0 20px rgba(0, 229, 195, 0.3);
  }

  .media-card:hover .zoom-hint {
    opacity: 1;
  }

  .video-wrapper {
    position: relative;
    width: 100%;
    padding-bottom: 56.25%;
  }

  .video-wrapper iframe {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    border: none;
  }

  .media-img {
    width: 100%;
    height: auto;
    display: block;
    transition: transform 0.3s ease;
  }

  .media-card:hover .media-img {
    transform: scale(1.03);
  }

  .media-caption {
    padding: 1rem;
    border-top: 1px solid #1a1a28;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .media-caption span {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.9rem;
    color: #00e5c3;
    font-weight: 600;
  }

  .zoom-hint {
    font-size: 0.75rem !important;
    color: #8585a0 !important;
    font-weight: 400 !important;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  /* Lightbox */
  .lightbox {
    position: fixed;
    inset: 0;
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    opacity: 0;
    visibility: hidden;
    transition: opacity 0.3s ease, visibility 0.3s ease;
  }

  .lightbox.active {
    opacity: 1;
    visibility: visible;
  }

  .lightbox-backdrop {
    position: absolute;
    inset: 0;
    background: rgba(10, 10, 20, 0.95);
    cursor: zoom-out;
  }

  .lightbox-close {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    z-index: 10;
    width: 48px;
    height: 48px;
    border: 2px solid #e8157a;
    background: rgba(10, 10, 20, 0.9);
    color: #00e5c3;
    font-size: 2rem;
    line-height: 1;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .lightbox-close:hover {
    background: #e8157a;
    color: #fff;
    box-shadow: 0 0 20px rgba(232, 21, 122, 0.6);
  }

  .lightbox-prev,
  .lightbox-next {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    z-index: 10;
    width: 48px;
    height: 64px;
    border: 2px solid #e8157a;
    background: rgba(10, 10, 20, 0.9);
    color: #00e5c3;
    font-size: 2rem;
    line-height: 1;
    cursor: pointer;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .lightbox-prev {
    left: 1.5rem;
  }

  .lightbox-next {
    right: 1.5rem;
  }

  .lightbox-prev:hover,
  .lightbox-next:hover {
    background: #e8157a;
    color: #fff;
    box-shadow: 0 0 20px rgba(232, 21, 122, 0.6);
  }

  .lightbox-content {
    position: relative;
    z-index: 5;
    max-width: 90vw;
    max-height: 90vh;
    animation: lightbox-zoom 0.3s ease;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  @keyframes lightbox-zoom {
    from {
      transform: scale(0.9);
      opacity: 0;
    }
    to {
      transform: scale(1);
      opacity: 1;
    }
  }

  .lightbox-img {
    max-width: 90vw;
    max-height: 80vh;
    width: auto;
    height: auto;
    object-fit: contain;
    border: 2px solid #e8157a;
    box-shadow: 0 0 40px rgba(232, 21, 122, 0.4), 0 0 80px rgba(0, 229, 195, 0.2);
  }

  .lightbox-caption {
    margin-top: 1rem;
    font-family: "IBM Plex Mono", monospace;
    font-size: 1rem;
    color: #00e5c3;
    text-transform: uppercase;
    letter-spacing: 0.15em;
  }

  /* Video Cards */
  .video-card {
    cursor: default;
  }

  .video-card:hover .media-img {
    transform: none;
  }

  /* See More Link */
  .see-more {
    margin-top: 2rem;
    text-align: center;
  }

  .see-more-link {
    display: inline-block;
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.9rem;
    color: #e8157a;
    letter-spacing: 0.1em;
    text-decoration: none;
    padding: 0.75rem 1.5rem;
    border: 1px solid #e8157a;
    transition: all 0.3s ease;
  }

  .see-more-link:hover {
    color: #fff;
    background: #e8157a;
    box-shadow: 0 0 20px rgba(232, 21, 122, 0.4);
  }

  /* Coming Soon */
  .coming-soon {
    border: 1px dashed #1a1a28;
    background: rgba(0, 0, 0, 0.3);
    padding: 3rem;
    text-align: center;
  }

  .coming-soon span {
    font-family: "IBM Plex Mono", monospace;
    font-size: 1rem;
    color: #9a9aa2;
    font-style: italic;
  }

  /* Responsive */
  @media (max-width: 720px) {
    .hero-title {
      font-size: 3rem;
    }

    .hero-top-bar {
      flex-wrap: wrap;
      justify-content: center;
    }

    .tagline {
      flex-direction: column;
      gap: 0.5rem;
    }

    .tag-line {
      display: none;
    }

    .content-section {
      padding: 2rem 1.25rem;
    }

    .section-head {
      flex-wrap: wrap;
      gap: 0.75rem;
    }

    .lightbox-prev,
    .lightbox-next {
      width: 40px;
      height: 50px;
      font-size: 1.5rem;
    }

    .lightbox-prev {
      left: 0.5rem;
    }

    .lightbox-next {
      right: 0.5rem;
    }

    .lightbox-close {
      width: 40px;
      height: 40px;
      top: 0.5rem;
      right: 0.5rem;
      font-size: 1.5rem;
    }

    .lightbox-caption {
      font-size: 0.85rem;
    }

    .num-block {
      width: 40px;
      height: 40px;
    }

    .num {
      font-size: 1.2rem;
    }

    .head-title {
      font-size: 1.4rem;
    }

    .section-body {
      padding-left: 0;
    }

    .media-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
